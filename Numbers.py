from math import *

my_num = 3.7
print(ceil(my_num))
# python can do +, -, *, /, % and ()
# abs() is for absolute
# pow(number, power)
# max(number1, number2) print out the largest number
# min(number1, number2) print out the smallest number
# round() round the number to the closest int
# from math import * to access all the math function
# ceil round the number up
# sqrt() return the square root of a number
